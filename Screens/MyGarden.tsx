import { Text,  View } from "react-native";
export default function MyGarden():JSX.Element {
    return (
      <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
        <Text>MyGarden</Text>
      </View>
    );
  }